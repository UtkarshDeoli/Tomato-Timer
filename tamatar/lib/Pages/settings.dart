import 'package:flutter/material.dart';

class settingPomo extends StatelessWidget {
  const settingPomo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Setting"),
      ),
      body: Center(child: Text("Coming Soon")),
    );
  }
}
